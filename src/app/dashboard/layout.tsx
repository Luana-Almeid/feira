
'use client';

// This layout is now simplified as the core structure is handled in the RootLayout
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>;
}
